# Ignore the virtual environment (local dependencies, not needed in repo)

venv/
.venv/

# Ignore local datasets and outputs (large + generated files)

dataset/
dataset_256/
aligned/
decoded_frames/

# Ignore logs (temporary/debug files)

logs/

# Ignore staging folder (temporary processing area for automation pipeline)

# This folder is used to safely copy and process files before final output

staging/

# Ignore Python cache files (auto-generated by Python)

**pycache**/
*.py[cod]
